const express = require('express')
const router = express.Router()

// Get all books
router.get('/', )
// Get book by Id
router.get('/:bookId')
// Add book
router.post('/', )
// Update book
router.patch('/:bookId', )
// Delete book
router.delete('/:bookId', )

module.exports = router